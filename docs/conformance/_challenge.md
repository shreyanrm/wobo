# The challenge — every tick that is not earned

**Role:** adversarial review of the seven registers in `docs/conformance/`. I did not re-audit the
product. I read what the seven auditors wrote and then went to the code to try to break their
**MET** rows, their **N/A** rows, and their coverage.

**Tree:** `/Users/depl/Documents/classess-learner`, branch `the-life`.
**HEAD at the time of this challenge:** `5ace319`. **The seven registers cite `0704817`** — two
commits behind. A wave was editing `apps/web-pwa/src` throughout; every file:line below was resolved
against the working tree on 2026-09-04 and is marked where the working tree differs from `HEAD`.

**Format.** The house format is `| # | Item | Standard | Status | Evidence | If not met |`. A
challenge needs a different fifth column, so the columns here are:

| # | Row challenged | What it claims | What the code actually does | Should be |

Sections 1–5 are the challenge. Section 6 records the ticks I attacked and **could not** break, so
the owner knows what survived. Section 7 is what I could not settle.

**Commands run for this document** (all from the repo root unless stated):

```
$ bun run typecheck                                   # exit 0, all 7 workspaces
$ bun run test                                        # web-pwa: 7 fail / 1611 pass; every other workspace green
$ cd apps/web-pwa && bun test src                     # x4, identical: 7 fail
$ cd apps/web-pwa && bunx playwright test tests/journey.spec.ts   # 3 failed of 3
$ cd services/gateway && uv run pytest tests/test_board_number_law.py tests/test_safety.py -q  # 26 passed
$ bun pm untrusted                                    # "Found 0 untrusted dependencies with scripts"
$ gh api repos/shreyanrm/wobo --jq .security_and_analysis
$ gh api repos/shreyanrm/wobo/branches/main/protection # 404 Branch not protected
$ curl -D - https://heywobo.com/ ; curl -D - "https://api.heywobo.com/v1/mail/stop?token=x"
$ curl -o /dev/null -w "%{http_code}" https://api.heywobo.com/v1/me/subscription   # 401
   Supabase MCP against project keepraxqagzgjrrweryt:
     list_migrations; pg_class/pg_policies/information_schema queries; get_advisors(security)
```

---

## 1. Ticks that are not earned

These are **MET** rows where I went to the code and the code does something else. Ordered by how
much it costs to be wrong.

| # | Row challenged | What it claims | What the code actually does | Should be |
|---|---|---|---|---|
| C1 | `content-and-safety.md` **1.2** — "A safety screen exists on the way OUT, before a model's words reach the child", and **1.11** — "The public 'Ask Wobo' box on the marketing site is screened (a child can type into it with no account)" | Both **MET**. 1.11's evidence: "`ask_public.py:1066-1073` classifies before counting and before retrieval" | The public box screens **inbound only**. `ask_public.py:1068` classifies the *question*; an unflagged question then reaches a frontier model through `gateway.invoke(CAPABILITY, …)` at `ask_public.py:1114`, and `CAPABILITY = "help.answer"` (`ask_public.py:68`). `help.answer` is **not** in `LEARNER_FACING_CAPABILITIES` (`safety.py:153-167`), and `screen_outbound` runs only `if capability in LEARNER_FACING_CAPABILITIES` (`app.py:341-343`). So the one surface a child with **no account and no age gate** can type into serves a model's words with no outbound safety screen at all — only the copy guards (provider names, links, pronouns) at `ask_public.py:1-27`. | **1.2 → PARTIAL**, **1.11 → PARTIAL**. Fix: add `"help.answer"` to `LEARNER_FACING_CAPABILITIES` — one line. |
| C2 | `content-and-safety.md` **1.2** again, and **4.9**'s framing | The outbound screen protects "whatever the model wants to put in front of the learner" | On the **board** path — the product's headline surface — the outbound screen is handed only the spoken line: `app.py:581-583` calls `screen_wobo_outbound({"say": plan.say, "actions": []}, gw.classifier)`. Every **drawn word** — every label, title, caption, `text` and `tex` on every board object, which `board/schema.py:234-241` (`visible_text`) itself enumerates as "everything on this object the learner can read" — goes to the child unscreened. `grep -rn "screen_outbound\|screen_wobo_outbound\|moderat" services/gateway/src/wobo_gateway/board/` returns **nothing**. | **PARTIAL**. Fix: pass the plan's `visible_text` through the same classifier before `build_events`. |
| C3 | `content-and-safety.md` **1.3** — "The screen covers every learner-facing surface, not just chat" | **MET**, on the strength of an 11-entry frozenset and a test that asserts the frozenset matches itself (`tests/test_safety.py:193`) | The same register's own item **1.9** says the curriculum intake is unscreened, and the code agrees: `app.py:932` short-circuits `if name.startswith("curriculum.")` **before** `Gateway.invoke`, so `curriculum.own.read` — which accepts a child's typed chapter list, a photographed page and a PDF, and sends them to a model (`curriculum/own.py:258-276`) — never meets either screen. Add `help.answer` (C1) and the frozenset is missing two of the surfaces a child actually types into. A row cannot be **MET** when a row 6 lines below it in the same register names the surface it misses. | **PARTIAL** |
| C4 | `content-and-safety.md` **4.8** — "The client refuses an unverified number at the wire, as a second gate" | **MET**. Evidence: `board-stream.ts:137,193-196` and the test `board-stream.test.ts:119` "refuses a number the verifier never passed, at the wire" | The cited lines are the `done` frame's `verified` **list**, not a gate. `dispatchFrame`'s `ink` case (`apps/web-pwa/src/wobo/board-stream.ts:167-172`) applies only the Zod grammar. I ran the probe: an object `{"kind":"number","value":25,"verified":false,"check":"cas.made_up"}` **passes the wire and reaches `onInk`** — output verbatim: `SEEN: [{"type":"ink","object":{"id":"n1",…,"value":25,"verified":false,…}}]`. The cited test passes only because it omits the `verified` key entirely, and `verified: z.boolean()` is required (`packages/wobo/src/board/schema.ts:149`). The real second gate is `isDrawable` (`packages/wobo/src/board/schema.ts:521-522`), called from `board/store.ts:195` — which the register never cites. | Keep **MET** (the control does exist) but **replace the evidence**. As written, this row is defended by a test that would still pass with the verified-number law deleted. See §3. |
| C5 | `application-security.md` **3.9** — "Logout terminates the session server-side" | **MET** | `packages/sdk/src/identity.ts:373-386`: `signOut` clears local state, then `await fetch(.../auth/v1/logout …)` inside a `try` with an **empty catch** whose own comment reads *"unreachable network — the revoke retries never"*. Two things follow. (a) On any network failure nothing is revoked and the row's claim is simply false. (b) Even on success, the gateway verifies the **access token** by signature and `exp` alone (`auth.py:179-185`) with no revocation check, so a bearer captured before sign-out keeps opening `/v1` for the rest of its TTL. | **PARTIAL** |
| C6 | `application-security.md` **1.3** / **4.6** — "RLS enabled and `subject_id = auth.uid()` policies on **every** learner table", citing `0014_subscriptions.sql:113-116`; `privacy-and-children.md` **J12** — "`learner.subscriptions` retained … deliberately"; `testing-and-quality.md` **8.4** — NOT VERIFIED | Both cite migration `0014` as evidence of a live control | **`learner.subscriptions` does not exist in the production database.** `select … from pg_class` over schemas `learner, curriculum, platform, public` returns 22 tables and no `subscriptions`. `list_migrations` on project `keepraxqagzgjrrweryt` returns **8** versions — `0001, 0006, 0007, 0008, 0009, 0010, 0011, 0012`. **`0013` and `0014` have never been applied**, and `information_schema.columns` confirms `learner.mastery_cache` has no `evidence` column. Every conformance claim resting on those two files is a claim about SQL that has never run. | The RLS half of **1.3/4.6** is still MET for the 22 tables that exist (I read every policy) — but the `0014` citation must go, **J12 → NOT MET (the table does not exist)**, and `testing 8.4` is now **verified: 2 of 14 migrations unapplied**. |
| C7 | `testing-and-quality.md` **3.2** — "The cancel/refund journey is covered end to end", standard given as *"consumer law: the exit must work"* | **MET**, run in CI as its own step | It is not end to end and it does not show the exit works. `apps/web-pwa/tests-plan/plan.spec.ts:37-38` stubs the answer: `page.route(\`${BRAIN}/v1/me/subscription\`, r => r.fulfill({ json: ACTIVE }))` and `…/subscription/cancel` likewise. `tests-plan/config.ts:18-23` launches with `VITE_LLM_MODE=mock`, `VITE_PERSIST_MODE=local`, `VITE_SUPABASE_URL=` — no gateway, no database. The suite asserts the **client's** two-tap dialog. Combined with C6 (the table the real route writes to does not exist in production), this is a green CI job standing in front of a feature that cannot work. | **PARTIAL** — "the client half of the cancel journey is covered; the server half and the store are covered by nothing." |
| C8 | `testing-and-quality.md` **3.1** — "The core learning journey is covered end to end" | **MET**. Evidence: "`tests/journey.spec.ts:47,119,152` — three tests walking landing → onboarding → home → course → boss → reward" | I ran it on the working tree: **3 failed of 3**. `journey.spec.ts:47` — `TimeoutError: locator.click: Timeout 15000ms exceeded` in onboarding; `:119` — `expect(locator('header').last()).toBeVisible()` → *element(s) not found*; `:152` — timeout before `heading "Learn"`. The register itself caveats "working tree red, HEAD green", but this is the row it uses to certify the product's central journey, and it is the row least able to carry a caveat. | **NOT VERIFIED at HEAD; NOT MET in the working tree.** Re-run in a clean `git worktree` of `HEAD` before this is ticked again. |
| C9 | `content-and-safety.md` **8.5** — "Promote before you invite" (and, by dependence, **8.1–8.4**) | **MET**, defended by `law-v5.test.ts:124-130` pinning every door to "Get early access" | The owner reversed the law in `5ace319`. `apps/web-pwa/src/screens/site/cta.ts:5` now reads *"'Get early access' is retired. The site says 'Start free'"*, and `:29` exports `START_FREE_LABEL = 'Start free'`. The defending test is **red** — `bun test src/screens/site/law-v5.test.ts` → `error: the product has not opened / + ["site/cta.ts: label: 'Start free',"]`. The register cannot tell the owner whether the code broke the law or the law moved, because the tick was a test result rather than a judgement. | **NOT MET as written.** The whole 8.x block needs re-basing on the conversion doctrine in `5ace319`. |
| C10 | `accessibility.md` **A13** — "Audio control (auto-playing audio > 3 s)" | **MET**. Evidence: `grep -rn "autoPlay\|…"` | The grep cannot see the product's actual audio. `apps/web-pwa/src/wobo/speech.tsx:4` — *"Wobo speaks what Wobo writes. **Every reply plays aloud**"* — and `SpeechNarrator` (`:669-691`) is *"always-mounted"*: an effect fires `void speakLine(last.text)` on **every** new Wobo turn, including the greeting a child has taken no action to request. `MotionPlayer.tsx:390` calls `void audio.play()` imperatively, which the grep for the `autoPlay` prop also misses. There is a mute switch, and it is unmuted by default (`profile.ts:225-231` returns `true` when the key is absent, and again when storage throws). | **PARTIAL.** The SC probably passes *because* a mute control exists — but the register's own **A78** proves the accessibility settings do nothing and **A86** could not find the settings panel at all, so the "mechanism" this SC turns on is unproven. |
| C11 | `application-security.md` **1.10/1.11** and `supply-chain` **2.1/2.17**; `privacy-and-children.md` **L1** ("Providers are identified internally — MET") and **H1** ("Transfers to model providers identified — MET as fact"); `content-and-safety.md` **7.18** ("no third-party script") | The set of third parties that receive a child's data is OpenAI, Anthropic, Google, Supabase, Railway, Vercel, Resend | **jsDelivr is missing from every one of them.** `apps/web-pwa/src/engines/cs/pyodide.ts:13-14` fetches ~10 MB of executable WASM from `https://cdn.jsdelivr.net/pyodide/v0.26.4/full/` at runtime, and `vercel.json:23` allows it in **both** `script-src` and `connect-src` (verified live on `https://heywobo.com/`). `grep -rniE "jsdelivr" docs/legal/` returns **nothing** — it is in no privacy policy, no cookies notice, no children's notice, and not on `/security`'s sub-processor table. `content-and-safety` **7.18** says "no third-party script" while its own **7.9**, nine rows earlier, quotes the CSP that permits one. | **L1/H1 → PARTIAL** (a live third-country recipient of a child's IP and user-agent is undisclosed). **7.18 → PARTIAL**, or reword to "no advertising or analytics script". app-sec **1.25/1.39** and supply-chain **1.15** already caught the *integrity* half honestly; only the *privacy* half was missed. |
| C12 | `privacy-and-children.md` **L1/H1** again, and `reliability`'s "FALSE PUBLIC CLAIM" finding | Two auditors caught `Security.tsx:194` (model providers "never knowing whose it is") and `:203-206` ("Crash reports and uptime checks") | The same list has **two more** entries nobody challenged, and a documents list with two more. `Security.tsx:198-201` publishes a **"Payments"** sub-processor that "take[s] the card or UPI payment on their own pages" — there is no payment processor; `screens/plans/Checkout.tsx:4-6` says *"The payment page is not open, so there is no checkout."* `Security.tsx:214` offers a **"Data processing agreement — for schools, on request"**; there is no DPA anywhere in the tree (`find . -name "*dpa*" -o -name "*data-processing*"` → nothing under `docs/`), and schools were deliberately removed from the product. `Security.tsx:215` offers the **"Data-flow diagram … as a PDF"**; `href="#flow"` is an in-page anchor (`:495`), not a PDF. `:174-177` still publish "Independent penetration test", "SOC 2 Type I, then Type II" and "ISO 27001 readiness" — honestly framed as future-tense, which is the fix the owner already made. | The `/security` sub-processor table and documents list are wrong in **both directions** — they name three things that do not exist and omit one that does (C11). No register owns this page's factual accuracy end to end; two registers each caught one line of it. |
| C13 | `privacy-and-children.md` **D8** — "Not subject to solely automated decisions with legal or significant effect", **MET as a matter of fact** | "no grading for a school, no admissions, no pricing by behaviour" | All three negatives are true, and none of them is the question. The product **does** decide, with no human in the loop, what a child is taught next: `grade.attempt` marks the answer, `mastery_cache.band` records the verdict, and `curriculum/placement.ts` decides where the child starts. Under the AADC and under DPDP s9(3) the argument that a learning path is not "similarly significant" for a child is exactly the argument counsel should be asked to make, not one an engineer should tick. The register's own `README.md` already assumes a DPIA is required. | **NOT VERIFIED** — "counsel's answer, recorded in the DPIA", rather than MET. |

---

## 2. N/A that is NOT MET wearing a disguise

| # | Row challenged | What it claims | What is really true | Should be |
|---|---|---|---|---|
| C14 | `supply-chain-and-operations.md` **9.10** — "Alert routes are tested" | **N/A** — "There is nothing to test — see §9.3, §9.4" | "The control does not exist, therefore testing it does not apply" is the definition of NOT MET, not of N/A. N/A is for a control that does not *apply to this product*. Applied consistently, every unbuilt control in every register could be N/A'd this way. The register half-admits it ("Re-evaluate the day a monitor exists"). | **NOT MET**, inheriting §9.4. |
| C15 | `supply-chain-and-operations.md` **10.3** — "A named person is responsible when it breaks" | **N/A (formally) / NOT MET (practically)** | A dual status is not one of the five the register's own key allows, and it lets the row be counted in the N/A column while the prose says NOT MET. `counts` records `na: 8`; this is one of them. | Pick one. **NOT MET.** |
| C16 | `privacy-and-children.md` **G7** — "The eraser button for content posted by a minor" (Cal. B&P §22581) | **N/A, near-miss** — "There is no public posting" | The row's own evidence describes a public posting: a learner offers their syllabus to the registry, and *"an accepted offer becomes a `community` framework with **no route back** for the learner who offered it"* (`0009_curriculum_review_offers.sql:13-15,52-54`). Content a minor submitted becomes visible to other users and cannot be withdrawn. That is the statute's fact pattern, minus the display of a name. | **PARTIAL** at best. |
| C17 | `content-and-safety.md` **1.14** — "The screen fails closed when the classifier is unavailable" | **N/A** — "there is no unavailable state to fail on" | Literally true and substantively misleading. The reason there is no failure state is that the classifier is a fifteen-phrase keyword list — which the same register's **1.4/2.5/2.6** show returns `ok` for *"my dad hits me"*, *"i havent eaten in three days on purpose"* and *"meet me after school, dont tell your mum"*. A control that cannot fail because it barely does anything should not be scored in the same column as a control that does not apply. | **N/A with the caveat promoted into the Status cell**, or NOT MET. |
| C18 | `accessibility.md` **A55** — "Consistent help", **MET (site) / N/A (app)** | "the signed-in app contains **no** help mechanism at all … so there is nothing to be inconsistent about" | Correct under the SC, which is conditional — but this N/A hides the fact that a child inside the product cannot reach help, while `content-and-safety` **5.7** and **6.1-6.4** are simultaneously ticking copy that tells that child to *"Flag it"* and *"use the flag in the corner"*. Two registers, opposite conclusions, neither cross-referenced. | Keep the SC verdict; add the cross-reference so the product gap is not laundered by a conformance N/A. |
| C19 | `application-security.md` **3.1 / 7.1** — the whole password family | **N/A** — "No password exists anywhere in the product" | True **today**, and one SDK method away from being false. The app ships a live password field with a label, an `autocomplete` token and an 8-character client-side minimum (`screens/auth/Auth.tsx:382,538-552`; `copy.ts:145`); it renders disabled only because `signInWithPassword` is absent from the SDK (`screens/auth/client.ts:27` vs `packages/sdk/src/identity.ts:51-68`, which exposes only `signInAnonymously`, `requestPhoneOtp`, `verifyPhoneOtp`, `signInWithGoogle`, `signOut`). Supabase's own advisor reports **`auth_leaked_password_protection` is disabled** on project `keepraxqagzgjrrweryt` (`get_advisors(security)`, observed 2026-09-04T10:34:27Z). The day that seam lands, the register says the family does not apply and the project setting says nobody checked. | **N/A today, with the trigger named** — and the leaked-password setting should be turned on now, before the door opens. |

---

## 3. Evidence that would survive the feature's deletion

The brief's third test: a **MET** defended by a test that would still pass if the thing it certifies
were removed. `testing-and-quality.md` measured this for itself (its items 2.4 and 3.2) and deserves
credit. These are the ones in the *other* registers.

| # | Row challenged | The evidence given | What survives deleting the feature |
|---|---|---|---|
| C20 | `content-and-safety.md` **4.8** | `board-stream.test.ts:119` "refuses a number the verifier never passed, at the wire" | Delete `isDrawable`'s verified check (`packages/wobo/src/board/schema.ts:522`) and delete the call at `board/store.ts:195`: **both** cited tests still pass, because they only exercise Zod's required-field check. Proved by probe — see C4. |
| C21 | `content-and-safety.md` **1.3** | `tests/test_safety.py:193` `test_the_crisis_screen_covers_every_learner_facing_capability` | The test asserts the frozenset in `safety.py:153` matches a list in the test file. It cannot detect a learner-facing capability that was never added to the frozenset — which is exactly the defect (`help.answer`, `curriculum.*`). A test that compares a constant to a copy of itself proves the copy is current, not that the set is complete. |
| C22 | `content-and-safety.md` **8.1–8.6**; `accessibility.md` **A54** | `law-v5.test.ts`, `identity.test.ts` — source-string matching over copy constants | These are the right shape for copy law and I am not calling them worthless. But they certify the **constants**, not the rendered page: `law-v5.test.ts` reads `page.text` from imported modules. Delete a page from the router and its constants still lint clean. `testing-and-quality.md` **7.8** already records the sitemap/router cross-check catching exactly this class of drift — that is the pattern the copy law needs too. |
| C23 | `application-security.md` **2.12** — "the dev impersonation header cannot exist in production", MET on "Five tests pin it" plus "`/docs` returns 404" | `tests/test_auth.py:128,141,154,161,168` | The five tests are the strongest evidence in the register and I could not break them. But the *live* half — "`/docs` returns 404, so `ENV=prod` is really set" — proves only that one env var is set. `DEV_AUTH` unset, `INTERNAL_EMAIL_KEY` set, `TRUST_PROXY=1` and `IP_LOG_SALT` are all still unproven; the register says so at item 8.7, and this row should point at that rather than borrowing the `/docs` result. |

---

## 4. Rows that were true at `0704817` and are false at `5ace319`

Not the auditors' fault — but the owner will read these registers as current, so they need a date
stamp and a re-run gate.

| # | Row | Was | Is now |
|---|---|---|---|
| C24 | `content-and-safety.md` **8.5** (and 8.1–8.4 by dependence) | MET | Red. See C9. |
| C25 | `testing-and-quality.md` **9.1** — "the unit suite is stable … 1425 pass / 0 fail" | MET | `bun run test`: **7 fail / 1611 pass** in `apps/web-pwa`; every other workspace green. Four consecutive `bun test src` runs gave an identical 7 fail, so the suite is **stable but red** — the stability claim holds, the green claim does not. The seven: five in `screens/plans/copy.test.ts` (monthly/yearly copy, checkout total, next-charge date), one in `copy.test.ts` "the consent boxes", one in `site/law-v5.test.ts`. |
| C26 | `testing-and-quality.md` **3.1** | MET | 3 failed of 3. See C8. |
| C27 | `testing-and-quality.md` **11.6** — "Typecheck passes" | MET | **Still MET.** `bun run typecheck` → all 7 workspaces exit 0. Recorded so the owner knows which of the three gates still holds. |
| C28 | *(new, uncovered)* the yearly auto-renewal consent box | — | `PLANS_PAGE.checkout.renewal.yearly` no longer contains "renews yearly"; it now reads *"I understand I am paying for a year, and I can cancel in Settings, in two taps."* An auto-renewal consent that never says the word *renews* is a consumer-law problem, not a copy problem. See §5. |

---

## 5. What nobody covered

I read the OWASP, WCAG 2.2 and DPDP lists myself against all seven registers.

**WCAG 2.2 — complete.** All 56 Level A and AA success criteria appear in `accessibility.md`
(1.1.1 → 4.1.3, including 4.1.1 recorded as removed). No criterion was missed. Credit where it is due.

**DPDP — complete.** s5 notice, s6 consent, s8(5) safeguards, s8(6) breach, s9 children, s10 SDF/DPO,
s11 access, s12 correction and erasure, s13 grievance redressal, s14 nomination, s6(7) Consent Manager
all appear in `privacy-and-children.md` (rows A5, A9, A13, B7, D2, E6–E11). No section was missed.

**OWASP** — Top 10 2021, API Top 10 2023 and ASVS 4.0 L2 are covered thoroughly. What is **not**
covered is the **OWASP Top 10 for LLM Applications**, which is the framework that actually matches
this product. LLM01 (prompt injection) is covered under A03 and covered well. LLM02 insecure output
handling is the C2/C4 gap. LLM06 sensitive information disclosure is the C11 gap. LLM08 excessive
agency (Wobo emits `actions` that mutate the page and the child's memory — `AppRuntime.tsx:726-735`)
is touched once and never scored. LLM04 model DoS is `application-security` 2.21 (no spend ceiling).
**Recommendation: one more section in `application-security.md` mapped to the LLM Top 10.**

Below is what falls outside all seven domains entirely.

| # | Uncovered area | Why it matters here | The smallest first step |
|---|---|---|---|
| C29 | **EU AI Act.** `grep -rniE "ai act\|artificial intelligence act\|annex iii" docs/` → **no matches**, anywhere in the repo. | Annex III(3) makes AI used in education to *evaluate learning outcomes* **high-risk**, and this product grades every attempt, computes a mastery band and places the learner. Art 5(1)(a)-(b) prohibits exploiting the vulnerabilities of children — which is the lens the streak flame (`ui/AppHeader.tsx:300-303`) and the rising per-day tone (`ui/sound.ts:164`) will be read through. Art 50 transparency is substantively met — `WOBO_INTRO` at `wobo.py:44-47` says *"I'm Wobo, your AI wobot"* on first meeting — but by accident, not by design. `Security.tsx:188-190` tells families data sits "with EU for EU families", so the EU is a declared market. | One page in `docs/legal/`: is Wobo an Annex III(3) system, and if so which obligations bite before an EU launch. Counsel, not an engineer. |
| C30 | **Consumer and distance-selling law.** No register names `docs/legal/refund-and-cancellation.md` or `docs/legal/terms-of-service.md`. | Wave 13 shipped "cancel, never refund", the plans page states a total and a next-charge date, and C28 shows the yearly auto-renewal consent box has just lost the word *renews*. India's Consumer Protection (E-Commerce) Rules 2020, the EU CRD Art 8(2) "order with obligation to pay" and the UK CCRs 14-day right all apply the moment checkout opens. Nobody has checked the copy against any of them. `refund-and-cancellation.md` carries **14** unresolved `[REVIEW]` markers and is live at `/legal/refunds`. | A consumer-law register, or a section in `content-and-safety.md`'s copy law, before the payment page opens. |
| C31 | **Four of the eleven published legal documents are audited by nobody.** `grep -rn "refund-and-cancellation\|terms-of-service\|acceptable-use\|accessibility-statement" docs/conformance/*.md` → **no matches**. | All four are live (`screens/legal/catalog.ts:18,21,22,26`) and carry **38** `[REVIEW]` markers between them (terms 13, refunds 14, accessibility 8, AUP 3) — and `content-and-safety` **7.5** proves `markdown.ts:166-168` strips every `[REVIEW]` block before rendering, so the caveats are invisible to the reader. The accessibility statement is the sharpest case: it is a **published conformance claim** (`accessibility-statement.md:17`) sitting beside a register that found 4 AA criteria NOT MET. It happens to be honest ("a statement of intent … not a claim of conformance") — but that is luck, not review. | Assign each of the four to a domain, or add an eighth register for the legal set as *published text*, separate from the privacy analysis. |
| C32 | **Content licensing and IP.** No register mentions copyright. | `curriculum/discovery/fetch.py` fetches a stranger's syllabus PDF, `extract.py` runs a model over it, and the product then teaches from it and stores it. `robots_allows` (`fetch.py:252-271`) is a politeness control, not a licence. India has no general TDM exception; §52 fair dealing is narrow. There is also no position on who owns a model's output that a family is being charged for. | One paragraph in the terms and one in `CURRICULUM.md`: what Wobo may ingest, under what basis, and what it will do on a takedown. |
| C33 | **Marketing-email law** — audited by nobody, and (unusually) **fine**. | `email_templates.py:32-58` names CAN-SPAM and DPDP by hand, ships a real postal address for Dot eVentures, and `:131-139` implements RFC 8058 `List-Unsubscribe` / `List-Unsubscribe-Post`. `/v1/mail/stop` is a working one-click opt-out. Recorded so the owner knows this is covered in code even though no register scores it. | One row in the privacy register pointing at `email_templates.py`. |

---

## 6. Ticks I attacked and could not break

The challenge is only useful if it says what held. I went at these specifically and failed.

| # | Row | How I tried to break it | Result |
|---|---|---|---|
| C34 | `application-security.md` **1.18** — HTML escaping in the gateway's server-rendered pages | Read every interpolation in both page builders | Holds. `parents.py:947-978` and `hospitality/api.py:114-120,244-248` escape title, line, action, token and button, with `quote=True` on every attribute. The one raw insertion (`{form}`) is built by the same escaped helper. |
| C35 | `application-security.md` **6.10/3.11** — logs carry no credential and no learner words | Looked for a query string in the request log, which would leak the mail-stop and invite tokens | Holds. `app.py:674` takes `request.url.path`, never `request.url`. `grep` for a logger call touching text/prompt/body across the whole service returns one unrelated warning. |
| C36 | `application-security.md` **1.19** — no XSS sink in the React app | `grep -rn "dangerouslySetInnerHTML\|innerHTML\|insertAdjacentHTML\|document.write"` over `apps/web-pwa/src` and `packages/*/src` | Holds. One hit, and it is the comment in `DiagramView.tsx:9` explaining why there is no sink. |
| C37 | `application-security.md` **1.3/4.6** — per-subject RLS | Queried the **live** database rather than the migrations | Holds for all 22 tables that exist. Every `learner.*` table carries a `subject_id = auth.uid()` (or `learner_id`) policy with a matching `with check`. `learner.content_cache`'s `qual: true` is the documented shared-content exception (`0002:64-66`). `curriculum.review_queue` has RLS on and zero policies — deny-all, as intended, and Supabase's advisor flags it only at INFO. **But see C6**: the row's `0014` citation is about SQL that never ran, and "every learner table" is imprecise while `content_cache` exists. |
| C38 | `supply-chain-and-operations.md` **1.10** — no third-party install scripts | `bun pm untrusted` | Holds, by a different route than the row's reasoning: *"Found 0 untrusted dependencies with scripts."* No package in the tree has an install script at all, so `trustedDependencies` never comes into it. |
| C39 | `supply-chain-and-operations.md` **2.14** — push protection | `gh api repos/shreyanrm/wobo --jq .security_and_analysis` | Holds: `secret_scanning: enabled`, `secret_scanning_push_protection: enabled`. Also confirms the register's other finding — `dependabot_security_updates: disabled` — and adds two the register missed: `secret_scanning_non_provider_patterns: disabled` and `secret_scanning_validity_checks: disabled`. |
| C40 | `application-security.md` **1.25/1.27** and `reliability` **1.9** — the live CSP | `curl -D - https://heywobo.com/` | Holds, and it is genuinely strong: `default-src 'self'`, `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, `frame-ancestors 'none'`, `upgrade-insecure-requests`, HSTS 2 years with `includeSubDomains`, `x-frame-options: DENY`, `nosniff`. `connect-src` does not name the Supabase host directly — it rides `'self'` through the `/db` rewrite, which is the right shape. |
| C41 | `application-security.md` **1.26** — the gateway's own HTML has no headers | Re-ran the curl | Confirmed exactly as the register states: `HTTP/2 400`, `content-type: text/html`, and **no** CSP, X-Frame-Options, nosniff or HSTS on `https://api.heywobo.com/v1/mail/stop?token=x`. |
| C42 | `testing-and-quality.md` **11.6** and the gateway suite | `bun run typecheck`; `uv run pytest tests/test_board_number_law.py tests/test_safety.py -q` | Both hold. Typecheck: 7/7 workspaces exit 0. Gateway: 26 passed. The board number law and the safety suite are green at HEAD. |
| C43 | `content-and-safety.md` **4.2** — the verified-number law server-side | Read `board/schema.py:519-523` and ran its suite | Holds, and it is the best-engineered control in the product. `contains_number(visible_text(obj))` with the check-name requirement, and the allowed set is the turn's own ledger (`board/planner.py:349-355`). My challenge is to the **client** half only (C4). |

---

## 7. What I could not settle

| # | Question | Why it is open | What would settle it |
|---|---|---|---|
| C44 | Whether `HEAD` (`5ace319`) is green, as distinct from the working tree | A wave was editing `apps/web-pwa/src` throughout; 40+ files are modified and uncommitted. Every red result in C25/C26 is a working-tree result. | `git worktree add ../wobo-head 5ace319 && bun install && bun run test && bunx playwright test` — the same fix `testing-and-quality.md` names for its own item 9.3. |
| C45 | Whether the live site at `heywobo.com` is built from this branch's current code | The production response carries `age: 15249` (≈4.2 h) and `x-vercel-cache: HIT`, and the entry chunk `index-CEfKqqf4.js` contains no `Start free`. So the deployed build predates `5ace319`. Every "live" measurement in all seven registers therefore describes a **different build** from the one their file:line citations describe. | Read the deployment SHA in the Vercel dashboard and record it beside each register's "verified live" rows. |
| C46 | Whether migrations `0002`–`0005` were ever recorded, or only `0013`/`0014` are genuinely missing | `list_migrations` returns 8 of 14 versions, but the tables from `0002` and `0005` **do** exist — so those were applied before the ledger began. Only `0013` and `0014` are provably unapplied (their objects are absent). | `supabase migration repair` for the pre-ledger four, then `supabase db push` for `0013`/`0014`, then re-run `list_migrations`. |
| C47 | Whether the phone-OTP and Google providers, and anonymous sign-in, are actually enabled on the Supabase project | The SDK wires exactly three seams (`identity.ts:51-68`) and nothing in the repo proves the project accepts them. No email or magic-link seam exists at all, which means the parent-email field at `Auth.tsx:566` and the under-13 consent path have **no send mechanism** — `recordParentalConsent` is a stub that throws (`identity.ts:67` declared, wired to `notEnabled` at `:261`, which raises `AuthNotEnabledError` at `:100-102`). | Supabase → Authentication → Providers, and one live under-13 sign-up walked end to end. This is a harder finding than `content-and-safety`'s "NOT VERIFIED — the magic-link flow runs against a dev seam"; the seam does not exist in the SDK at all. |
| C48 | Whether `curriculum.own.read`'s image path can carry a prompt written *inside* a photographed page | I confirmed the bypass (C3) but not the exploit. `own.py:431` frames the extracted text as data; nothing frames the **image**. | One red-team item: photograph a page with an instruction written on it and see what Wobo does. It belongs in the same suite `content-and-safety` already names as its highest-value missing test. |
| C49 | Whether any of the seven registers' "live" HTTP measurements would still hold under the current deploy | Follows from C45. | Re-run each register's `curl` block against the deployment named in C45 and date-stamp the result. |
