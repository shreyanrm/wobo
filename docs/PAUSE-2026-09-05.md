# PAUSE — 2026-09-05

Read this first on resume. Everything below is verified, not remembered.

## State at the pause

- **Working tree clean.** Nothing uncommitted.
- **Green:** 7 packages typecheck, 2,866 app tests, gateway suite, all 3 brand gates.
- **22 commits on `the-life`, none pushed.** `origin/the-life` is still at `31b31d5`.
- **No wave is running.** All eight finished and landed.
- Dev server stopped. Start it with `cd apps/web-pwa && bun run dev` at http://localhost:5173.

## Done on production today, and it mattered

**Migrations 0013 and 0014 are applied.** They close a live privilege hole: `learner.profiles_cache`
had a blanket UPDATE grant under a `for all` policy, so any signed-in learner could PATCH their own
row and hand themselves a paid plan, or raise their own consent tier, which is a capability door.
UPDATE is now granted per column on the seven the client legitimately writes. Verified after
applying: `can_still_write_plan = false`. `learner.subscriptions` now exists.

**(Since superseded: 0015-0018 went in the same day, and 0019 and 0020 on 2026-09-05. Nothing in
the repository is unapplied. `docs/OPERATIONS.md` §8 is the current record.)** As written then:
**Still unapplied: 0015, 0016, 0017, 0018** (admin register and audit, the skipped number, the
reports queue, the usage ledger). The console and the ledger have no tables behind them until these
land. Bodies with comments stripped are at `/tmp/00*.min.sql`, or apply from the repo files. There is
no Supabase CLI or psql on this machine and the project is not linked, so they went through the
Supabase MCP tool one at a time.

## The owner's standing decisions from this session

- **The product first, the landing pages later.** *"once that is ready and flawless we will have
  enough knowledge to context to build the perfect landing pages."* Every public-page task is PAUSED.
  Do not diff `screens/{landing,site,pitch,plans,gift,donate,contact}` or `design/prototypes`.
- **No anonymous demo.** CANCELLED, not deferred: *"no demo without account cause it is free
  anyways."*
- **Consent deferred to the end.** Still the largest item in the privacy register and still required
  before a real launch.
- **Payments: Razorpay.** Nothing wired yet. Needs the owner's keys.
- **Operations: authorised.** *"do whatever is needed."*
- **The hero line is "Fall in love with learning while studying."** and the category claim is
  *"The world's first AI companion that shows you"*, mid-page. Both are the owner's, both were
  argued against by me and reaffirmed by him, and both are settled. `docs/CLAIMS.md` substantiates
  the second element by element. Do not reopen either; extend CLAIMS.md instead.
- **Never name a late hour**, enforced by `hours.test.ts`.
- **A doubt is any time; learning is not the night before.** We are not a cramming tool.

## The next wave, written and NOT launched

`/private/tmp/.../scratchpad/waves/wave20-product.js` is complete and ready. Three builders, two
adversaries, one fixer:

1. **Is the teaching actually right.** The owner's headline ask, and the product's central claim has
   no test at all today: all 5,400 tests are about the plumbing around the teaching. Build the
   harness, run it, fix what it finds. It must check the DRAWING and the TEACHING, not only the
   words, because a right answer with an empty board is the failure a text-only harness misses.
2. **Never lose a child's work.** Five swallowed save failures under a loader whose last word is
   "Your place is saved"; `mastery_cache` has never successfully synced for anyone; no client
   timeout on any gateway call, proved to hang past 45s; and "start over" reaches 6 of 22 stores.
3. **The flag control and the board's voice.** The report queue, endpoint and admin desk all exist;
   the control a child taps does not, while the help centre promises one on every lesson, question,
   board and diagram. And five of eight objects on a real Pythagoras board are announced to a screen
   reader as nothing, on a product whose premise is that it draws the explanation.

Launch with `Workflow({scriptPath: ".../waves/wave20-product.js"})`.

## Two things waiting on the owner

1. **Push, or not.** 22 commits including the security fixes.
2. **The three Vercel variables.** `VITE_DEV_AUTH`, `VITE_PERSIST_MODE`, `VITE_LLM_MODE` all exist
   and all pull empty, so the deployed bundle carries no Supabase URL and no gateway URL. heywobo.com
   is a demo shell: it cannot sign anyone in, reach the tutor, or save anything. Four accounts exist
   from July testing and every learner table has zero rows.

## The crisis-response decision, still open

What happens when a child discloses harm is shipped at the safest option: the turn never reaches a
model, Wobo says the calm line and stays, the lesson stops, and the child is never told they were
flagged. **Whether a linked parent is ever told is unresolved and is the owner's call with counsel.**
Telling a guardian carries the most legal weight in every market we sell to and is also the option
that can hurt the exact child the screen just found, because the probe that started this work is
`my dad hits me`.

## Live at 14:20 (four runs, all builders writing)

- `wf_5b2d02c9-074` — Wave 25: syllabi to production, discovery worker, 121 verified, beat-driven voice, the observer
- `wf_5184126e-aaf` — Wave 23: onboarding (no duplicate sign-in, no silent submit)
- `wf_900f6a92-eb1` — Wave 22: the voice and the screen (draw on screen, lock scroll, screenshot only for video)
- `wf_7fd3e47f-082` — Wave 24: the doubt solver (photo a page, Wobo annotates and explains stroke by sentence)

Resume any of them after a stop with `Workflow({scriptPath, resumeFromRunId})`; scripts live in
`scratchpad/waves/`. Wave 25 was stopped at 14:16 and resumed with a fourth builder (the syllabus
observer, `docs/CURRICULUM-OBSERVER.md`, task §10.18) and a poisoning attack on it.
